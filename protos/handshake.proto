syntax = "proto3";
package touchpad.v1;

import "error_code.proto";

// 服务器端响应握手请求
message Welcome {
  bytes cert_der = 1; // 后续通讯的公钥
  uint64 ts_ms = 2;
}

// 连接推出信号
message Exit {
  uint64 ts_ms = 1;
}

message Reject {
  ErrorCode reason = 1;
}
